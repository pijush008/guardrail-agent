# Agent core
openai>=1.0
PyYAML>=6.0
pytest>=7.0
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
pypdf>=4.0
python-multipart>=0.0.9

# PII redaction (NER-backed, precision-filtered; deterministic regex still
# runs first). Also requires the spacy model, installed separately with:
#     python -m spacy download en_core_web_lg
presidio-analyzer>=2.2
presidio-anonymizer>=2.2

# Transactional confirmation emails (real SMTP when SMTP_HOST/USER/PASSWORD
# are set, else a simulated sender keeps the permission gate working offline).
#   sendgrid>=6.11  # optional alternative to smtplib
